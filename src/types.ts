export type Service = {
  id: string;
  title: string;
  description: string;
  icon: string;
};

export type TeamMember = {
  id: string;
  name: string;
  role: string;
  image: string;
  bio: string;
};

export type Testimonial = {
  id: string;
  name: string;
  university: string;
  content: string;
  image: string;
};